# 2026-04-01 去背景后重生成提示文案收口

## 问题
去背景完成后，用户继续修改宽度、色系或颜色风格时，弹窗仍然提示“重新生成会覆盖这些修改”，容易让用户误以为去背景后就不能再继续编辑。

## 修改
将 EditorPage 中所有重生成确认提示统一改成：
“重新生成会按新的参数重新计算图案。已去背景区域会尽量保留，但其他手动编辑可能被覆盖，确定继续吗？”

## 结果
- 用户可以明确知道：去背景结果会尽量保留。
- 同时保留对其他手动编辑可能被覆盖的风险提示。
- 减少对“去背景后不能继续编辑”的误解。
