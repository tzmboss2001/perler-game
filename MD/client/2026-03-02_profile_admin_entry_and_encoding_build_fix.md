# 个人页管理员入口与历史乱码编译修复

日期：2026-03-02

## 问题
- ProfilePage 中历史乱码导致多处字符串/标签断裂，`vite build` 失败。
- 缺少审核台入口，管理员无法从前端进入审核流程。

## 修复
1. 在个人页菜单加入管理员入口“社区审核台”。
2. 修复 ProfilePage 中断裂字符串和 JSX 标签（退出按钮、提示文案、分享按钮 title、菜单箭头等）。

## 验证
- `cmd /c npm run build` 通过。

## 影响
- 管理员可从个人页进入审核台。
- 前端构建稳定恢复。
