# 制作模式移除区块选择提示

## 问题
- 制作模式里，点击区块后会弹出 `选中区块 (x, y)` 的提示框。
- 这类提示不会帮助用户继续制作，反而会不断累加，遮挡浏览区和工具区。

## 修改
- 移除了 `MakingPage.tsx` 中区块选择时的两处 `toast.info(...)`：
  - 鼠标点击区块
  - 触摸点击区块

## 保留
- 真正有价值的提示仍然保留：
  - 板完成/取消完成
  - 库存不足
  - 分享/导出反馈

## 结果
- 选择区块时不再弹出“选中区块”信息框。
- 复位、换色、坐标等实际操作能力不受影响。
