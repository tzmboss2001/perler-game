# 2026-03-22 背景处理模式确认按钮收口

## 变更目的
- 将背景处理模式中的主确认动作改成更直白、更像主按钮的形态。
- 避免“应用去背景”语义过长、样式不够像确认按钮。

## 本次修改
1. 将主按钮文案从“应用去背景”改为“确定”。
2. 同步更新提示文案中的按钮名称。
3. 强化主按钮样式：增大高度、宽度、字号、主色渐变和投影，使其与普通功能按钮区分开。

## 涉及文件
- `perler-beads/src/pages/mobile/EditorPage.tsx`

## 验证
- `npm run build` 通过。
