# 2026-03-04 第6项优化：主链路自动化回归并接入发布卡口

## 目标
将 `Create -> Editor -> Making` 的核心前端主链路纳入发布前自动检查，做到“改动即回归，失败即阻断”。

## 本次改动
1. 新增主链路契约回归脚本
- 文件：`TEST/mobile_main_flow_contract.ps1`
- 检查内容（ASCII 契约，避免中文编码误报）：
  - Router 存在 `create / editor / making` 三条关键路由
  - Create 页保留快路径与高级切换关键逻辑（`fastMode`、`showAdvanced`、进入 editor）
  - Editor 页保留主动作和次动作关键逻辑（`handleStartMakingClick`、`handleShareClick`、`setShowSaveModal(true)`）
  - Making 页核心组件仍存在

2. 接入发布卡口
- 文件：`SCRIPT/frontend_quality_gate.ps1`
- 新增步骤：`frontend_main_flow_contract`
- 已纳入 `npm run quality:gate` 默认执行流程。

## 验证结果
- 命令：`npm.cmd run quality:gate`
- 报告：`TEMP/quality_gate_report.md`
- 结果：
  - `frontend_build: PASS`
  - `frontend_dev_smoke: PASS`
  - `frontend_main_flow_contract: PASS`
  - `frontend_garbled_diff: PASS`

## 过程说明
- 初版使用中文文案匹配，在 Windows PowerShell 脚本编码环境下出现误报。
- 已调整为 ASCII 结构契约检查，稳定通过。