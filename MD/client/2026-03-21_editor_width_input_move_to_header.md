# 2026-03-21 编辑页宽度输入移到标题行

## 问题
- 编辑图案页宽度控件的当前数值输入框放在滑杆下方，会额外占一行空间。
- 用户希望把下方那个 `104` 数值直接放到“宽度”标题旁边，减少垂直占用。

## 修改
- 将宽度输入框从滑杆下方移到标题行右侧。
- 删除原本单独占一行的宽度输入区域。
- 保留原有输入、回车确认和失焦确认逻辑。

## 影响文件
- `perler-beads/src/pages/mobile/EditorPage.tsx`

## 验证
- 执行 `cmd /c npm run build`，构建通过。
