# 背景处理模式底部按钮 sticky 可见性修复

时间：2026-04-03

## 问题
背景处理模式中，虽然 `回退一步 / 确定 / 一键去背景` 已渲染，但在部分屏幕高度下仍会掉到首屏下方，用户看不到。

## 修复
1. 保持背景处理覆盖层可纵向滚动。
2. 缩放区禁止收缩，避免被挤压变形。
3. 底部操作区改为 `position: sticky`，固定在去背景页可视区域底部上方。
4. 继续保留底部导航安全区偏移，避免被 TabBar 压住。

## 结果
背景处理模式中，底部三个主按钮会稳定出现在当前视口底部附近，不再掉到首屏外。
