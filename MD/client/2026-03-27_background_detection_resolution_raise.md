# 2026-03-27 去背景检测分辨率提升

## 本次修改
- 文件：`perler-beads/src/pages/mobile/EditorPage.tsx`
- 将背景检测专用网格宽度保底值从 `104` 提升到 `208`。
- 仅影响背景检测链路，不改变编辑页当前显示宽度。
- 一键去背景会在更高分辨率网格上先识别背景，再映射回当前编辑网格。

## 修改原因
- 对蓝天、树叶、渐变背景这类复杂背景，`104` 宽度仍然过粗，边缘背景被拆散后很难进入自动背景候选。
- 提高到 `208` 后，自动去背景更有机会识别出连续的大块外部背景，而不是只圈出 1 格。
