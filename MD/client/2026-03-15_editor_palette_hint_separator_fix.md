# 编辑页色系说明分隔符修复

- 日期：2026-03-15
- 范围：客户端

## 问题

修复“色系按钮点击黑屏”后，MCP 继续复测发现色系面板说明文案中间显示成了 `?`：
- `基础配色 ? 像素画、图标`

这是实际界面文案错误，不是快照误判。

## 修复

- 将 `EditorPage.tsx` 中色系说明分隔符从 `?` 改为 `·`

## 结果

- 色系说明恢复为正常中文排版
- 面板文案不再显得像编码错误
