# 2026-04-01 背景处理模式桌面端平移入口恢复

## 修改目的
- 修复背景处理模式在桌面端无法平移预览图的问题。
- 恢复“移动画面/选背景”角标按钮，避免因为入口被隐藏导致鼠标只能点选不能平移。

## 修改内容
1. `EditorPage.tsx`
- 去掉背景处理模式中对 `isCoarsePointer` 的条件限制。
- 预览区右上角的小型切换按钮现在始终显示。
- 用户在桌面端点击 `移动画面` 后即可拖动画布，再点击 `选背景` 切回点格子模式。

## 结果
- 桌面端不再依赖隐藏手势或额外说明才能平移。
- 移动入口仍然是小角标按钮，不会重新占用整行空间。
