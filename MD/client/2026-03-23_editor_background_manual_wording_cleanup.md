# 2026-03-23 背景处理模式文案收口

## 改动目标
把“高级微调”这类偏技术的说法，改成用户一眼能看懂的“手动扣背景”，降低理解成本。

## 本次改动
- 将背景处理模式中的 `高级微调` 统一改为 `手动扣背景`
- 将 `打开高级微调` 改为 `打开手动扣背景`
- 将 `收起高级微调` 改为 `收起手动扣背景`
- 将说明文案中的“微调补救”改成“手动扣背景补干净”
- 将辅助提示改成“只有自动结果不满意时，再用下面的手动扣背景”

## 影响范围
- 客户端编辑页背景处理模式
- 文件：`perler-beads/src/pages/mobile/EditorPage.tsx`

## 验证
- 执行 `npm run build`
- 构建通过

## 结果
背景处理模式现在对普通用户更直白：
- `一键去背景` 表示自动处理
- `手动扣背景` 表示用户自己补救
