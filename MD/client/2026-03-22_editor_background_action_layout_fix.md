# 2026-03-22 背景处理模式底部按钮布局收口

## 变更目的
- 解决背景处理模式中“返回编辑”被自动换行挤到单独一行的问题。
- 将底部操作条改成更稳定的两层布局。

## 本次修改
1. 将底部操作条从自动换行布局改成两层明确结构。
2. 第一层放回退、前进、清空选择。
3. 第二层放主确认按钮“确定”和“返回编辑”。
4. 固定“返回编辑”按钮宽度，避免再次被布局挤压掉行。

## 涉及文件
- `perler-beads/src/pages/mobile/EditorPage.tsx`

## 验证
- `npm run build` 通过。
