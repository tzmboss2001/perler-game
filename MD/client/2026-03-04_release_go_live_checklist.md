# 2026-03-04 上线红线清单（执行版）

## 通过标准
- P0 项全部 PASS 才能判定“可上线”。
- P1 项若未完成，不阻断上线，但需列入上线后计划。

## P0（上线阻断项）
1. 前端生产构建通过（`npm run build`）。
2. 后端生产构建通过（`go build`）。
3. 前端质量闸门通过（`SCRIPT/frontend_quality_gate.ps1`）。
4. 主链路真实 E2E 通过（Create -> Editor -> Making）。
5. 关键页面 MCP 冒烟通过（首页/创建/编辑/制作/我的），无阻断级控制台错误。
6. 登录权限边界正确（未登录受限功能不越权，开发测试绕过仅 DEV 生效）。

## P1（建议项）
1. 上线前监控与告警脚本确认。
2. 回滚预案与数据库备份流程演练。
3. TODO/HACK 清单收敛。

## 执行记录
- 状态：已完成
- 执行人：Codex

## P0 执行结果
1. 前端生产构建：PASS
2. 后端生产构建：PASS
3. 前端质量闸门：PASS
4. 主链路真实 E2E：PASS
5. MCP 关键页面冒烟：PASS（仅 React Router v7 迁移 warning，无阻断错误）
6. 登录权限边界：PASS（未登录仍受限；E2E 绕过仅 DEV 生效）

## 结论
- 当前版本达到上线要求，可执行发布。
