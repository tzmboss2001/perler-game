# 编辑页宽度滑杆补充 208 标记并优化引导线

## 本次修改

1. 将编辑页宽度滑杆最大值从 `200` 提升到 `240`
2. 在标准拼豆板宽度标记中新增 `208`
3. 优化滑杆标记引导线样式：
   - 缩短连线长度，避免超出按钮区域
   - 从 `1px` 提升到 `2px`
   - 改成更清晰的青色高对比线
4. 微调标记区域上下留白，保证四个标记按钮与连线显示更稳定

## 影响文件

- `perler-beads/src/pages/mobile/EditorPage.tsx`

## 验证

- 执行 `cmd /c npm run build`
- 构建通过
