# 智能抠图接入骨架

## 时间
- 2026-03-11

## 本次改动
- 新增 `aiCutoutService.ts`，统一承载智能抠图的供应商计划、接入状态和后续请求入口。
- 编辑图案页的“智能抠图”按钮不再只弹一条 toast，而是改为显示结构化状态弹窗。
- 弹窗会明确展示：
  - 当前服务商规划；
  - 推荐解锁方式；
  - 当前是否已接通；
  - 下一步需要接入什么。

## 目的
- 先把前端产品入口和工程接口固定下来。
- 后续无论接阿里云还是其他方案，只需要补服务端 API，不用重做编辑页交互。

## 当前状态
- 前端入口已准备好。
- 当前仍是本地占位状态，尚未发起真实 AI 请求。

## 验证计划
- 本地执行 `npm run build`。
- 使用 MCP 打开编辑图案页，点击“智能抠图”，确认弹出状态弹窗且内容完整。
