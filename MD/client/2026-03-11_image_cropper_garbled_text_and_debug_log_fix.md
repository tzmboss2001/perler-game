# 2026-03-11 ImageCropper 乱码与调试日志清理

## 本次问题
- `ImageCropper.tsx` 里存在用户可见乱码文案，且其中一处 JSX 文本损坏到了标签结构，直接导致前端构建失败。
- 裁剪组件里还残留 `Canvas size` 和 `Calculated pixelCrop` 调试日志，会污染控制台。

## 本次修改
- 修复裁剪页用户可见文案：
  - `裁剪图片`
  - `宽高比`
  - `原图`
  - `推荐`
  - `处理中...`
  - `确认`
- 移除裁剪组件中的调试日志输出。
- 修正裁剪失败时的错误日志文案。

## 关键文件
- `perler-beads/src/components/ImageCropper.tsx`

## 验证
- `npm run build` 已通过。
- 本地 MCP 重新上传图片后，已正常进入 `裁剪图片` 页面，并能看到修复后的文案。

## 说明
- 这轮只做本地修复，没有发布公网。
