# 背景处理模式底部操作区遮挡缩放区修复

时间：2026-04-03

## 问题
背景处理模式下，底部 `回退一步 / 确定 / 一键去背景` 操作区使用固定定位悬浮在页面底部，导致上方的 `预览缩放` 控件在窄屏下被遮挡，用户无法正常操作缩放按钮和滑杆。

## 修复
1. 背景处理模式覆盖层取消额外的底部占位 padding。
2. 底部操作区从 fixed 悬浮布局改成页面内部正常布局。
3. 保留底部安全区内边距，但不再压住缩放区。
4. 阴影减弱，避免看起来像浮层覆盖在控件上。

## 结果
背景处理模式中，`预览缩放` 区和底部主按钮区会按页面正常顺序排列，不再互相遮挡。
