# 2026-03-21 编辑页新增镜像翻转入口

## 变更内容
- 在编辑图案页左侧悬浮工具区新增 `镜像 / 翻转` 按钮，位置放在 `风格 / 颜色` 下方。
- 新增镜像翻转面板，提供：
  - `左右镜像`
  - `上下镜像`
- 镜像操作直接作用于当前图案，并写入回退历史。
- 左侧悬浮面板改为三者互斥：
  - 色系
  - 风格
  - 镜像
- 点击面板外部空白区域会关闭当前悬浮面板。

## 修改文件
- `perler-beads/src/pages/mobile/EditorPage.tsx`

## 验证
- 运行 `cmd /c npm run build`
