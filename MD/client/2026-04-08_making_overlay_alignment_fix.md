# 2026-04-08 ?????????????

## ??
????????????????????????????

## ??
overlay ????? `baseCanvas.getBoundingClientRect()` ??????????????? `translate + scale` ? `transform-origin: center`?????????????????????????????????????

## ??
- ?? overlay ? `getBoundingClientRect()` ???
- ????????????? `renderMetrics`?`translateX`?`translateY`
- ??????? overlay ??????
  - `visualCanvasWidth = safeRenderCanvasWidth * displayScale`
  - `visualCanvasHeight = safeRenderCanvasHeight * displayScale`
  - `canvasLeft = (wrapperWidth - visualCanvasWidth) / 2 + translateX`
  - `canvasTop = (wrapperHeight - visualCanvasHeight) / 2 + translateY`

## ??
??????????????????????????????????
