# 2026-03-04 第5项优化：Editor 文案可读性清理（第二轮）

## 目标
继续降低编辑页理解成本，让用户一眼知道“先做什么、按钮是什么意思”。

## 本次改动
- 文件：`perler-beads/src/pages/mobile/EditorPage.tsx`

1. 统计区动作文案更直白
- `换` -> `换色`
- `原` -> `还原`

2. 增加主流程提示条
- 在主按钮上方新增提示：
  - `完成调整后，点击“保存并开始制作”进入制作模式`
- 目的：明确唯一主路径，减少犹豫。

## MCP 回归结果
- 页面：`/mobile/editor`
- 结果：
  - 主提示条正常显示
  - 统计区按钮显示为 `换色`
  - 主按钮与次级入口保持：`保存并开始制作` / `分享图纸`

## 构建与门禁
- `npm.cmd run build`：通过
- `npm.cmd run quality:gate`：通过

## 备注
- 首次单独 build 遇到一次 `dist/models/Avocado.glb` 文件占用（EBUSY），重试后通过；质量闸门中 build 步骤最终为 PASS。