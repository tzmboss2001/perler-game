# 2026-03-21 编辑页镜像功能简化

## 变更内容
- 将编辑图案页的镜像功能简化为单一的 `左右镜像`。
- 删除 `上下镜像` 入口，避免用户做多余选择。
- 镜像面板说明改为：点一次左右颠倒，再点一次恢复。
- 保持镜像结果写入回退历史。

## 修改文件
- `perler-beads/src/pages/mobile/EditorPage.tsx`

## 验证
- 运行 `cmd /c npm run build`
