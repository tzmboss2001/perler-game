# 手机端单板模式总览图入口文案优化

## 问题
手机端单板模式中，缩略总览图被默认收起后，只显示“总览”按钮。

用户容易理解成缩略图已经没有了，而不是“点击后展开”。

## 修复
- 将手机端单板模式的总览按钮文案从：
  - `总览`
- 改为：
  - `看总览图`
- 展开后的按钮文案从：
  - `收起`
- 改为：
  - `收起总览`
- 同时加宽按钮最小宽度，避免文案被挤压。

## 修改文件
- `perler-beads/src/pages/mobile/MakingPage.tsx`

## 验证
- `npm run build` 通过
