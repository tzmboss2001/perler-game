# 全局协作规则（实时生效）

更新时间：2026-03-03

1. 不再杀任何 `chrome` 或 `node` 相关进程。
2. 只在现有页面内测试：不新建页面、不关闭页面、不重启页面。
3. 使用 MCP 测试时，严格遵循以上两条。


## 2026-03-06 新增全局规则（服务器操作）
- 进行服务器远程操作时，优先使用 Python + paramiko 执行 SSH/SFTP，不使用临时手工交互命令作为主流程。
- 涉及发布时，先执行连接验证，再执行部署，再做线上可访问性检查。
